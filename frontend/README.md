src/
├── api/           # Configuración de Axios
├── assets/
├── components/    # Componentes reutilizables (Navbar, Layout, Inputs)
├── hooks/         # Custom hooks
├── modules/       # Módulos por dominio (Auth, Transactions, etc.)
├── pages/         # Páginas completas (LoginPage, Dashboard)
├── routes/        # Definición de rutas y protecciones
├── store/         # Estado global (Zustand)
├── types/         # Interfaces compartidas (podés copiar las del backend)
└── App.tsx